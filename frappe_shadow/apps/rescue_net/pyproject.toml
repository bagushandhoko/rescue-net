[project]
name = "rescue_net"
authors = [{ name = "Rescue-Net", email = "bagushandhoko@gmail.com" }]
description = "Rescue-Net shadow migration app for Frappe"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core"]
build-backend = "flit_core.buildapi"
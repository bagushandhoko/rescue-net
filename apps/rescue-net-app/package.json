{
  "name": "rescue-net-app",
  "version": "0.1.0",
  "private": true,
  "description": "Rescue-Net cross-platform app shell for Android, iOS, and desktop.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check src/app.js && node --check sw.js && node --check server.js",
    "prepare:store": "node scripts/prepare-store.js"
  },
  "capacitor": {
    "appId": "org.rescuenet.app",
    "appName": "Rescue-Net",
    "webDir": ".",
    "server": {
      "androidScheme": "https"
    }
  }
}


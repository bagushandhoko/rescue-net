"""Shared business rules that DocType controllers call (architecture review
phase 2). API modules check input + permission and shape the response; the
rules that must ALWAYS hold — whatever path saves the document — live here
and in the controllers."""
